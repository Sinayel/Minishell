#include <stdio.h>
#include <stdlib.h>

typedef struct s_token
{
	char			*token;
}					t_token;

typedef struct s_data
{
	struct s_token  *token;
}					t_data;


int main()
{
    t_token *list;
    t_data *data;
    list->token = malloc(sizeof(char) * 10);
    list->token = "caca";
    printf("t_token : %s\n", list->token);
    printf("t_token : %s\n", data->token->token);

}

FTSTRLENparenthesisopen the parenthesisjump lineoK
Int iEqual zero
WhileSTRI
I plus plus
ReturnII